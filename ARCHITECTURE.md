# 🏗 Архитектура приложения

```
┌─────────────────────────────────────────────────────────────┐
│                      TELEGRAM USER                           │
│                           ↓                                  │
│                    Telegram Bot                              │
│                           ↓                                  │
│              ┌────────────┴────────────┐                    │
│              │                         │                     │
│         /start                   Open Mini App              │
│              ↓                         ↓                     │
│    ┌─────────────────┐      ┌──────────────────┐           │
│    │  Bot Messages   │      │   Frontend (Web)  │           │
│    │   Welcome &     │      │   React + Vite    │           │
│    │  Reminders      │      │   TailwindCSS     │           │
│    └─────────────────┘      └──────────────────┘           │
│              ↑                         ↓                     │
│              │                    API Calls                  │
│              │                         ↓                     │
│    ┌─────────────────────────────────────────────┐         │
│    │           Backend (Node.js)                  │         │
│    │  ┌──────────────────────────────────────┐   │         │
│    │  │  Express API Server                  │   │         │
│    │  │  - /api/publications (CRUD)          │   │         │
│    │  │  - /api/notifications (History)      │   │         │
│    │  └──────────────────────────────────────┘   │         │
│    │                                              │         │
│    │  ┌──────────────────────────────────────┐   │         │
│    │  │  Telegram Bot API                    │   │         │
│    │  │  - Handles /start command            │   │         │
│    │  │  - Sends reminders                   │   │         │
│    │  └──────────────────────────────────────┘   │         │
│    │                                              │         │
│    │  ┌──────────────────────────────────────┐   │         │
│    │  │  Notification Service                │   │         │
│    │  │  - Checks deadlines hourly           │   │         │
│    │  │  - Sends reminders (9:00 AM daily)   │   │         │
│    │  └──────────────────────────────────────┘   │         │
│    │                                              │         │
│    │  ┌──────────────────────────────────────┐   │         │
│    │  │  SQLite Database                     │   │         │
│    │  │  Tables:                             │   │         │
│    │  │  - users                             │   │         │
│    │  │  - publications                      │   │         │
│    │  │  - notifications                     │   │         │
│    │  └──────────────────────────────────────┘   │         │
│    └─────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Поток данных

### Добавление публикации
1. User открывает Mini App
2. Frontend отправляет данные → POST /api/publications
3. Backend сохраняет в SQLite
4. Frontend обновляет список

### Отправка напоминаний
1. Cron запускается каждый час (и в 9:00)
2. NotificationService проверяет deadlines
3. Если осталось 1 день или 0 дней → отправить сообщение
4. Сохранить в таблицу notifications

## 🔧 Технологический стек

### Frontend (Vercel)
- React 18
- Vite (build tool)
- TailwindCSS
- Telegram Mini App SDK
- Lucide Icons

### Backend (Railway)
- Node.js 18+
- Express.js
- node-telegram-bot-api
- SQLite3
- node-cron
- CORS

## 🌐 Переменные окружения

### Backend (Railway)
```env
BOT_TOKEN=xxxxx          # От @BotFather
FRONTEND_URL=https://... # Vercel URL
PORT=3000                # Railway автоматически
```

### Frontend (Vercel)
```env
VITE_API_URL=https://... # Railway URL + /api
```

## 📅 Логика напоминаний

```javascript
// Проверка каждый час
if (daysUntilEnd === 1) {
  sendReminder(publication, 'one_day_before');
}

if (daysUntilEnd === 0) {
  sendReminder(publication, 'end_day');
}
```

## 🔒 Безопасность

- CORS настроен на FRONTEND_URL
- Валидация userId в каждом запросе
- Soft delete для публикаций (is_active = 0)
- Environment variables для секретов
