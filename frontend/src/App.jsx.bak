import { useState, useEffect } from 'react';
import { Plus, Calendar, Users, TrendingUp, Bell, Trash2, Edit2, Check, X, Menu } from 'lucide-react';

// Демо данные
const DEMO_PUBLICATIONS = [
  {
    id: 1,
    publisher: 'Иван Петров',
    groups: 'Продажи авто, Барахолка Москва',
    publications_per_day: 3,
    end_date: '2026-02-02',
    created_at: '2026-01-25 10:00:00',
    is_active: 1
  },
  {
    id: 2,
    publisher: 'Мария Сидорова',
    groups: 'Недвижимость СПб, Аренда квартир',
    publications_per_day: 2,
    end_date: '2026-02-05',
    created_at: '2026-01-26 14:30:00',
    is_active: 1
  },
  {
    id: 3,
    publisher: 'ООО "Ремонт+"',
    groups: 'Ремонт и строительство',
    publications_per_day: 5,
    end_date: '2026-02-01',
    created_at: '2026-01-20 09:15:00',
    is_active: 1
  }
];

const DEMO_NOTIFICATIONS = [
  {
    id: 1,
    publication_id: 3,
    user_id: 123456789,
    type: 'end_day',
    sent_at: '2026-02-01 09:00:00',
    publisher: 'ООО "Ремонт+"',
    groups: 'Ремонт и строительство',
    end_date: '2026-02-01'
  },
  {
    id: 2,
    publication_id: 1,
    user_id: 123456789,
    type: 'one_day_before',
    sent_at: '2026-02-01 09:00:00',
    publisher: 'Иван Петров',
    groups: 'Продажи авто, Барахолка Москва',
    end_date: '2026-02-02'
  }
];

function App() {
  const [publications, setPublications] = useState(DEMO_PUBLICATIONS);
  const [notifications, setNotifications] = useState(DEMO_NOTIFICATIONS);
  const [activeTab, setActiveTab] = useState('publications');
  const [showAddForm, setShowAddForm] = useState(false);
  const [editingId, setEditingId] = useState(null);
  
  const [formData, setFormData] = useState({
    publisher: '',
    groups: '',
    publicationsPerDay: '',
    endDate: ''
  });

  const handleSubmit = (e) => {
    e.preventDefault();
    
    if (editingId) {
      // Обновление
      setPublications(publications.map(pub => 
        pub.id === editingId 
          ? { 
              ...pub, 
              publisher: formData.publisher,
              groups: formData.groups,
              publications_per_day: parseInt(formData.publicationsPerDay),
              end_date: formData.endDate
            }
          : pub
      ));
    } else {
      // Создание
      const newPub = {
        id: Math.max(...publications.map(p => p.id)) + 1,
        publisher: formData.publisher,
        groups: formData.groups,
        publications_per_day: parseInt(formData.publicationsPerDay),
        end_date: formData.endDate,
        created_at: new Date().toISOString(),
        is_active: 1
      };
      setPublications([...publications, newPub]);
    }
    
    resetForm();
  };

  const handleDelete = (id) => {
    if (window.confirm('Вы уверены, что хотите удалить эту публикацию?')) {
      setPublications(publications.filter(pub => pub.id !== id));
    }
  };

  const handleEdit = (publication) => {
    setFormData({
      publisher: publication.publisher,
      groups: publication.groups,
      publicationsPerDay: publication.publications_per_day.toString(),
      endDate: publication.end_date
    });
    setEditingId(publication.id);
    setShowAddForm(true);
  };

  const resetForm = () => {
    setFormData({
      publisher: '',
      groups: '',
      publicationsPerDay: '',
      endDate: ''
    });
    setEditingId(null);
    setShowAddForm(false);
  };

  const getDaysRemaining = (endDate) => {
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const end = new Date(endDate);
    end.setHours(0, 0, 0, 0);
    const days = Math.floor((end - today) / (1000 * 60 * 60 * 24));
    return days;
  };

  const formatDate = (dateString) => {
    return new Date(dateString).toLocaleDateString('ru-RU', {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric'
    });
  };

  const formatDateTime = (dateString) => {
    return new Date(dateString).toLocaleString('ru-RU', {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit'
    });
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      {/* Header */}
      <div className="bg-gradient-to-r from-blue-600 to-indigo-600 text-white p-4 sticky top-0 z-10 shadow-lg">
        <div className="max-w-4xl mx-auto">
          <h1 className="text-xl font-bold flex items-center gap-2">
            <Bell size={24} className="animate-pulse" />
            Напоминания о публикациях
          </h1>
          <p className="text-sm text-blue-100 mt-1">Демо режим - все данные только для примера</p>
        </div>
      </div>

      {/* Tabs */}
      <div className="bg-white border-b sticky top-[72px] z-10 shadow-sm">
        <div className="max-w-4xl mx-auto flex">
          <button
            onClick={() => setActiveTab('publications')}
            className={`flex-1 py-4 px-4 font-medium transition-all ${
              activeTab === 'publications'
                ? 'text-blue-600 border-b-2 border-blue-600 bg-blue-50'
                : 'text-gray-600 hover:bg-gray-50'
            }`}
          >
            📋 Публикации ({publications.length})
          </button>
          <button
            onClick={() => setActiveTab('history')}
            className={`flex-1 py-4 px-4 font-medium transition-all ${
              activeTab === 'history'
                ? 'text-blue-600 border-b-2 border-blue-600 bg-blue-50'
                : 'text-gray-600 hover:bg-gray-50'
            }`}
          >
            📝 История ({notifications.length})
          </button>
        </div>
      </div>

      {/* Content */}
      <div className="max-w-4xl mx-auto p-4 pb-20">
        {activeTab === 'publications' && (
          <>
            {/* Add Button */}
            {!showAddForm && (
              <button
                onClick={() => setShowAddForm(true)}
                className="w-full bg-gradient-to-r from-blue-600 to-indigo-600 text-white py-4 rounded-xl font-medium flex items-center justify-center gap-2 mb-4 shadow-lg hover:shadow-xl transform hover:scale-[1.02] transition-all"
              >
                <Plus size={20} />
                Добавить публикацию
              </button>
            )}

            {/* Add/Edit Form */}
            {showAddForm && (
              <div className="bg-white rounded-xl shadow-xl p-6 mb-6 border border-gray-100">
                <h2 className="text-xl font-bold mb-4 text-gray-800 flex items-center gap-2">
                  {editingId ? (
                    <>
                      <Edit2 size={20} className="text-blue-600" />
                      Редактировать публикацию
                    </>
                  ) : (
                    <>
                      <Plus size={20} className="text-green-600" />
                      Новая публикация
                    </>
                  )}
                </h2>
                <form onSubmit={handleSubmit} className="space-y-4">
                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">
                      👤 Кто публикуется
                    </label>
                    <input
                      type="text"
                      required
                      value={formData.publisher}
                      onChange={(e) => setFormData({ ...formData, publisher: e.target.value })}
                      className="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
                      placeholder="Имя или название компании"
                    />
                  </div>

                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">
                      📢 В каких группах
                    </label>
                    <input
                      type="text"
                      required
                      value={formData.groups}
                      onChange={(e) => setFormData({ ...formData, groups: e.target.value })}
                      className="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
                      placeholder="Названия групп через запятую"
                    />
                  </div>

                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">
                      📊 Публикаций в день
                    </label>
                    <input
                      type="number"
                      required
                      min="1"
                      value={formData.publicationsPerDay}
                      onChange={(e) => setFormData({ ...formData, publicationsPerDay: e.target.value })}
                      className="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
                      placeholder="Количество публикаций"
                    />
                  </div>

                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">
                      📅 Конечная дата
                    </label>
                    <input
                      type="date"
                      required
                      value={formData.endDate}
                      onChange={(e) => setFormData({ ...formData, endDate: e.target.value })}
                      className="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
                    />
                  </div>

                  <div className="flex gap-3 pt-2">
                    <button
                      type="submit"
                      className="flex-1 bg-gradient-to-r from-green-600 to-emerald-600 text-white py-3 rounded-lg font-semibold flex items-center justify-center gap-2 hover:shadow-lg transform hover:scale-[1.02] transition-all"
                    >
                      <Check size={18} />
                      {editingId ? 'Сохранить' : 'Добавить'}
                    </button>
                    <button
                      type="button"
                      onClick={resetForm}
                      className="flex-1 bg-gray-200 text-gray-700 py-3 rounded-lg font-semibold flex items-center justify-center gap-2 hover:bg-gray-300 transition-all"
                    >
                      <X size={18} />
                      Отмена
                    </button>
                  </div>
                </form>
              </div>
            )}

            {/* Publications List */}
            {publications.length === 0 ? (
              <div className="text-center py-16 text-gray-500 bg-white rounded-xl shadow-md">
                <Bell size={64} className="mx-auto mb-4 opacity-30" />
                <p className="text-lg font-medium">Пока нет публикаций</p>
                <p className="text-sm mt-2">Добавьте первую публикацию!</p>
              </div>
            ) : (
              <div className="space-y-4">
                {publications.map((pub) => {
                  const daysRemaining = getDaysRemaining(pub.end_date);
                  const isUrgent = daysRemaining <= 1;
                  
                  return (
                    <div
                      key={pub.id}
                      className={`bg-white rounded-xl shadow-lg p-5 transform hover:scale-[1.01] transition-all ${
                        isUrgent ? 'border-l-4 border-red-500 shadow-red-100' : 'border-l-4 border-blue-500'
                      }`}
                    >
                      <div className="flex justify-between items-start mb-4">
                        <div className="flex-1">
                          <h3 className="font-bold text-lg text-gray-800 mb-1">{pub.publisher}</h3>
                          <div className="flex items-center gap-2 text-sm text-gray-600">
                            <Users size={16} className="text-blue-500" />
                            <span>{pub.groups}</span>
                          </div>
                        </div>
                        <div className="flex gap-2">
                          <button
                            onClick={() => handleEdit(pub)}
                            className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg transition-all"
                            title="Редактировать"
                          >
                            <Edit2 size={18} />
                          </button>
                          <button
                            onClick={() => handleDelete(pub.id)}
                            className="p-2 text-red-600 hover:bg-red-50 rounded-lg transition-all"
                            title="Удалить"
                          >
                            <Trash2 size={18} />
                          </button>
                        </div>
                      </div>

                      <div className="grid grid-cols-2 gap-4 mb-4">
                        <div className="flex items-center gap-2 text-sm bg-gray-50 p-3 rounded-lg">
                          <TrendingUp size={16} className="text-purple-500" />
                          <div>
                            <div className="text-xs text-gray-500">В день</div>
                            <div className="font-semibold">{pub.publications_per_day} раз</div>
                          </div>
                        </div>
                        <div className="flex items-center gap-2 text-sm bg-gray-50 p-3 rounded-lg">
                          <Calendar size={16} className="text-orange-500" />
                          <div>
                            <div className="text-xs text-gray-500">Конец</div>
                            <div className="font-semibold">{formatDate(pub.end_date)}</div>
                          </div>
                        </div>
                      </div>

                      <div className={`p-3 rounded-lg text-sm font-semibold text-center ${
                        daysRemaining < 0
                          ? 'bg-gray-200 text-gray-600'
                          : daysRemaining === 0
                          ? 'bg-gradient-to-r from-red-500 to-pink-500 text-white animate-pulse'
                          : daysRemaining === 1
                          ? 'bg-gradient-to-r from-orange-500 to-amber-500 text-white'
                          : 'bg-gradient-to-r from-green-500 to-emerald-500 text-white'
                      }`}>
                        {daysRemaining < 0
                          ? '❌ Срок истёк'
                          : daysRemaining === 0
                          ? '🔥 ПОСЛЕДНИЙ ДЕНЬ!'
                          : daysRemaining === 1
                          ? '⚠️ Завтра последний день'
                          : `✅ Осталось ${daysRemaining} ${daysRemaining === 1 ? 'день' : daysRemaining < 5 ? 'дня' : 'дней'}`
                        }
                      </div>
                    </div>
                  );
                })}
              </div>
            )}
          </>
        )}

        {activeTab === 'history' && (
          <>
            {notifications.length === 0 ? (
              <div className="text-center py-16 text-gray-500 bg-white rounded-xl shadow-md">
                <Bell size={64} className="mx-auto mb-4 opacity-30" />
                <p className="text-lg font-medium">История уведомлений пуста</p>
                <p className="text-sm mt-2">Здесь будут отображаться отправленные напоминания</p>
              </div>
            ) : (
              <div className="space-y-4">
                {notifications.map((notif) => (
                  <div key={notif.id} className="bg-white rounded-xl shadow-lg p-5 hover:shadow-xl transition-all">
                    <div className="flex items-start gap-4">
                      <div className="text-4xl">
                        {notif.type === 'one_day_before' ? '⏰' : '🔔'}
                      </div>
                      <div className="flex-1">
                        <div className="flex items-center gap-2 mb-2">
                          <span className={`px-3 py-1 rounded-full text-xs font-semibold ${
                            notif.type === 'one_day_before'
                              ? 'bg-orange-100 text-orange-700'
                              : 'bg-red-100 text-red-700'
                          }`}>
                            {notif.type === 'one_day_before'
                              ? 'За 1 день'
                              : 'День окончания'
                            }
                          </span>
                        </div>
                        <h4 className="font-bold text-gray-800 mb-1">{notif.publisher}</h4>
                        <p className="text-sm text-gray-600 mb-2 flex items-center gap-1">
                          <Users size={14} />
                          {notif.groups}
                        </p>
                        <div className="flex items-center gap-3 text-xs text-gray-500 mt-3">
                          <span className="flex items-center gap-1">
                            <Calendar size={12} />
                            Конец: {formatDate(notif.end_date)}
                          </span>
                          <span className="flex items-center gap-1">
                            <Bell size={12} />
                            Отправлено: {formatDateTime(notif.sent_at)}
                          </span>
                        </div>
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            )}
          </>
        )}
      </div>

      {/* Info Banner */}
      <div className="fixed bottom-0 left-0 right-0 bg-gradient-to-r from-blue-600 to-indigo-600 text-white p-3 shadow-lg">
        <div className="max-w-4xl mx-auto text-center text-sm">
          <p className="font-medium">💡 Это демо-версия приложения</p>
          <p className="text-xs text-blue-100 mt-1">
            Напоминания приходят за 1 день до окончания и в день окончания публикации
          </p>
        </div>
      </div>
    </div>
  );
}

export default App;